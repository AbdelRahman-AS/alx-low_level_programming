#include "main.h"
/**
 * _isdigit - Returns 1 if digit, 0 if non digit.
 * @c: Int to check if digit
 * Return: Always 0.
 */
int _isdigit(int c)
{
if (c > 47 && c < 58)
{
return (1);
}
else
{
return (0);
}
}
