#include "main.h"
/**
 * _isupper - Returns 1 if upper case, 0 if lower.
 * @d: Char to check if digit
 * Return: Always 0.
 */
int _isdigit(char d)
{
if (d > 47 && d < 58)
{
return (1);
}
else
{
return (0);
}
}
