#include "main.h"
/**
 * _isupper - Returns 1 if upper case, 0 if lower.
 * @c: Int to check if digit
 * Return: Always 0.
 */
int _isdigit(int c)
{
if (c > 47 && c < 58)
{
return (1);
}
else
{
return (0);
}
}
