#include "main.h"

/**
 * _strcmp - compares two strings
 * @a: string
 * @n: int
 *
 * Return: void
 */

void reverse_array(int *a, int n)
{
	int i;
	int x;

	for (i = 0; ; i++)
		if (s1[i] == s2[i])
		{
			x = 0;
			if (s1[i] == '\0')
				break;
		}
		else
		{
			x = s1[i] - s2[i];
			break;
		}
}

